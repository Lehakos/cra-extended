// @flow
import React, { PureComponent } from 'react';

{{#if wantPropTypes}}
import type { Props } from './{{ pascalCase name }}.types';
{{/if}}
{{#if wantStyles}}
import s from './{{ pascalCase name }}.style';
{{/if}}

// eslint-disable-next-line react/prefer-stateless-function
export class {{ pascalCase name }} extends PureComponent<*> {

  render() {
    return (
      <div>
        {{ pascalCase name }}
      </div>
    );
  }

}
