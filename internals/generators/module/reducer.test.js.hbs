import reducer{{pascalCase name}} from '../reducer.{{camelCase name}}';

describe('{{upperCase name}} module reducers', () => {
  it('returns the initial state', () => {
    expect(reducer{{pascalCase name}}(undefined, {})).toEqual({});
  });
});
