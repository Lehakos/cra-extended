// import * as selectors from '../selectors.{{camelCase name}}';

describe('{{upperCase name}} module selectors', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
